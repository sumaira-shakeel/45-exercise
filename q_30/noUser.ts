let userName:string[] = ['saira','yusra','nadia','pashmeena','palwasha'];
if(userName.length === 0){
    console.log('we need to find some users!');

}else{
    userName=[];
    console.log('All user name as been removed.' +userName.length)
}